[build]
backend = "python"
params = { "railwaypythonxref" = "3.11" }

[deploy]
build = "cd frontend && npm install && npm run build"

[postBuildCommand]
command = "rm -rf backend/static && cp -r frontend/dist backend/static"